// greet.miru
// Say hello to every name in a list.
// Run with: miru run examples/greet.miru

fn greet(name) {
  return "Hello, " + name + "!"
}

let names = ["Aiko", "Ken"]
for n in names {
  print(greet(n))
}
